<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>com.portfolio</groupId>
    <artifactId>portfolio-chatbot-backend</artifactId>
    <version>1.0.0</version>
    <name>Portfolio Chatbot Backend</name>
    <description>
        Standalone Spring Boot RAG chatbot for the portfolio site.
        Deployed as a separate AWS Lambda *outside* the VPC so it can reach
        api.openai.com directly. Holds no DB connection — its knowledge base
        comes from bundled markdown (resources/knowledge/*.md and the
        repo-level docs/*.md). The portfolio-backend Lambda continues to
        serve every other /api/* route.
    </description>

    <properties>
        <java.version>21</java.version>
        <!-- Override Spring Boot 3.5.14's managed Tomcat 10.1.53 to the
             CVE-2026-34487 / CVE-2026-34483 fix. Literal version so Trivy resolves it. -->
        <tomcat.version>10.1.54</tomcat.version>
        <spring-ai.version>1.1.7</spring-ai.version>
        <!-- Matches the AWS SDK v2 BOM used by the other backends. -->
        <aws-sdk.version>2.46.15</aws-sdk.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
                Import the AWS SDK BOM BEFORE spring-ai-bom: spring-ai-bom also
                manages software.amazon.awssdk:* (for its optional Bedrock
                support) and pins an older 2.20.x. The first-declared BOM wins,
                so listing ours first keeps every awssdk submodule on a single,
                consistent ${aws-sdk.version} matching the other backends.
            -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws-sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Servlet MVC stack (the /api/chatbot/* endpoints are plain REST). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Bean validation on @RequestBody DTOs. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- AWS Lambda glue. -->
        <dependency>
            <groupId>com.amazonaws.serverless</groupId>
            <artifactId>aws-serverless-java-container-springboot3</artifactId>
            <version>2.1.5</version>
        </dependency>

        <!--
            Spring AI: OpenAI chat + embeddings. We exclude every transitive
            webflux artefact for the same reason as portfolio-backend — its
            presence on the classpath causes Spring Boot to register reactive
            content-negotiation that silently nukes JSON response bodies on
            the servlet path. Spring AI's RestClient code path handles all our
            non-streaming calls.
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-webflux</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-openai</artifactId>
            <version>${spring-ai.version}</version>
        </dependency>

        <!-- VectorStore interface + SimpleVectorStore (file-backed, in-process). -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-vector-store</artifactId>
        </dependency>

        <!--
            AWS Secrets Manager: the OpenAI API key is fetched at startup from
            Secrets Manager (see OpenAiKeyResolver) instead of being injected as
            a plaintext Lambda env var. We swap the default Apache sync client
            for the dependency-free url-connection-client — one GetSecretValue
            call at init needs nothing heavier, and it keeps the shaded Lambda
            jar small.
        -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>secretsmanager</artifactId>
            <exclusions>
                <!-- We use only the sync client via url-connection-client, so
                     drop both default HTTP clients: the Apache sync client and
                     the Netty async client (unused, and a frequent CVE source). -->
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!--
            We *share* the curated knowledge files with the soon-to-be-trimmed
            portfolio-backend module so a single canonical copy lives under
            portfolio-backend/src/main/resources/knowledge. We also bundle the
            repo-level /docs directory so the bot has the full architecture
            documentation available in production.
        -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>${project.basedir}/../portfolio-backend/src/main/resources/knowledge</directory>
                <targetPath>knowledge</targetPath>
                <includes>
                    <include>*.md</include>
                </includes>
            </resource>
            <resource>
                <directory>${project.basedir}/../docs</directory>
                <targetPath>docs</targetPath>
                <includes>
                    <include>*.md</include>
                </includes>
            </resource>
        </resources>

        <plugins>
            <!-- Lambda-compatible flat JAR. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.2</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- ${argLine} is populated by JaCoCo's prepare-agent goal at runtime -->
                    <argLine>${argLine}
                        -XX:+EnableDynamicAgentLoading
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                        -Dnet.bytebuddy.experimental=true
                    </argLine>
                </configuration>
            </plugin>

            <!-- JaCoCo for code coverage -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
